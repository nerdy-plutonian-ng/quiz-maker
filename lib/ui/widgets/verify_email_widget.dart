import 'package:flutter/material.dart';

class VerifyEmailWidget extends StatelessWidget {
  const VerifyEmailWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [Text('Verify Email')],
    );
  }
}
