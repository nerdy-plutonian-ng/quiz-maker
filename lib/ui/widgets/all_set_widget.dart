import 'package:flutter/material.dart';

class AllSetWidget extends StatelessWidget {
  const AllSetWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [Text('All Set')],
    );
  }
}
